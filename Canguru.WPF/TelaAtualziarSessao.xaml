<Window x:Class="Canguru.WPF.AtualizarSessao"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Atualizar Sessão" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#E68933">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Painel lateral -->
        <Border Background="#151527" CornerRadius="0,20,20,0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="A" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="T" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="U" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="A" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="L" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="I" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="Z" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="A" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
                <TextBlock Text="R" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,8,0,8"/>
            </StackPanel>
        </Border>

        <!-- Conteúdo principal -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <Border Background="White" CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Width="300" Height="60" Grid.Row="0">
                <TextBlock Text="Atualizar sessão"
                           Foreground="#1E1B2E"
                           FontSize="20"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Área de formulário -->
            <Border Grid.Row="1" Background="White" CornerRadius="20" Padding="40" Margin="10,10,30,31">
                <StackPanel Margin="0,-30,0,45">
                    <TextBlock Text="Nome da sessão:" FontWeight="Bold" FontSize="14" Margin="5,0,0,5"/>
                    <TextBox x:Name="txtNomeSessao" Height="45" BorderBrush="Gray" BorderThickness="1" Padding="10"/>

                    <TextBlock Text="Nova descrição:" FontWeight="Bold" FontSize="14" Margin="5,20,0,5"/>
                    <TextBox x:Name="txtDescricaoSessao" Height="107" TextWrapping="Wrap" AcceptsReturn="True" BorderBrush="Gray" BorderThickness="1" Padding="10"/>

                    <!-- Botões -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,5" Width="238">
                        <Button x:Name="btnCancelar" Content="Cancelar" Width="100" Height="35" 
                                Background="#E57373" Foreground="White" BorderThickness="0"
                                 Margin="10,0,0,0" Click="btnCancelar_Click"/>
                        <Button x:Name="btnSalvar" Content="Salvar" Width="100" Height="35"
                                Background="#81C784" Foreground="White" BorderThickness="0"
                                 Margin="10,0,0,0" Click="btnSalvar_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
