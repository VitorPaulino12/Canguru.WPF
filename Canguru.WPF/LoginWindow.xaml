<Window x:Class="Canguru.WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Canguru.WPF"
        mc:Ignorable="d"
        Title="CANGURUU - Login"
        Height="720"
        Width="350" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Loaded="Window_Loaded">

    <Grid Width="300" Height="500">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 
        ESTE É O BORDER QUE FALTAVA (CS0103)
        -->
        <Border x:Name="LoadingOverlay" Background="#80000000" CornerRadius="10"
                Visibility="Collapsed" Margin="0,10,10,154">
            <TextBlock Text="Carregando..." Foreground="White" FontSize="20"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Background="{StaticResource CorLaranjaPrincipal}"
                CornerRadius="10,10,10,10" Margin="0,10,10,154">
            <StackPanel VerticalAlignment="Center" Margin="30">

                <TextBlock
                            FontSize="28"
                            FontWeight="Bold" HorizontalAlignment="Center"
                            >
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="2" Direction="315" Color="DarkGray" Opacity="0.5" BlurRadius="3"/>
                    </TextBlock.Effect><Run Text="CANG" Foreground="Black"/><Run Text="URUU" Foreground="White"/></TextBlock>

                <!-- ALTEREI O LABEL PARA INDICAR QUE PODE SER EMAIL OU RA -->
                <Label Content="Login (E-mail ou RA)" Foreground="White" FontSize="14"/>
                <TextBox x:Name="txtLogin" Margin="0,5,0,10" TextChanged="txtLogin_TextChanged"/>

                <Label Content="Senha" Foreground="White" FontSize="14"/>
                <PasswordBox x:Name="txtSenha" Margin="0,5,0,50" KeyDown="txtSenha_KeyDown"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BtnCadastrar" Grid.Column="0" Content="Cadastrar" Margin="0,0,5,0"
                            Click="BtnCadastrar_Click"/>

                    <Button x:Name="BtnEntrar" Grid.Column="1" Content="Entrar" Margin="5,0,0,0"
                            Click="BtnEntrar_Click" IsDefault="True"/>
                </Grid>

                <Button x:Name="BtnEsqueceuSenha" 
                        Content="Esqueceu a senha?" 
                        Margin="0,15,0,0"
                        Click="BtnEsqueceuSenha_Click"
                        Background="Transparent" 
                        BorderBrush="Transparent"
                        Foreground="White"
                        Cursor="Hand"
                        FontSize="12">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="LightGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>