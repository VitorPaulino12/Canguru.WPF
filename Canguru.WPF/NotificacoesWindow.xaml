<Window x:Class="Canguru.WPF.NotificacoesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Canguru.WPF"
        mc:Ignorable="d"
        Title="NotificacoesWindow"
        Height="450"
        Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#1A1A2E">

        <!-- BOTÃO X PARA FECHAR A JANELA -->
        <Button Content="X"
                Width="40"
                Height="40"
                FontSize="20"
                FontWeight="Bold"
                Foreground="White"
                Background="{StaticResource CorLaranjaPrincipal}"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,10,10,0"
                Click="BtnFechar_Click"/>

        <!-- Header -->
        <Border Background="White"
                Height="90"
                VerticalAlignment="Top"
                CornerRadius="15"
                Margin="180,70,180,0">
            <TextBlock Text="Notificações"
                       VerticalAlignment="Center"
                       FontSize="36"
                       FontWeight="SemiBold"
                       Foreground="#1A1A2E"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Main Content Area -->
        <Border Background="{StaticResource CorLaranjaPrincipal}"
                CornerRadius="25"
                Margin="10,180,10,10">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="stkNotificacoes"
                            Margin="20"
                            Orientation="Vertical"
                            VerticalAlignment="Top"/>
            </ScrollViewer>

        </Border>

    </Grid>
</Window>
